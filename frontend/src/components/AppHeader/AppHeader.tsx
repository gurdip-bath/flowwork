import React from 'react';
import AuthContext from "../../context/AuthContext";

// CURRENT TO DO - use React doc to create the app header component. style it with tailwind and add the logo.

function AppHeader () {
    return (
        <div>
            <h1 className='bold'>Welcome Mikey</h1>
        </div>
    )
}